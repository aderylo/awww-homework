# awww-homework

## How to? 
If you have running postgres instance and npm installed then follow these steps. 

First, install dependecies: 
```
cd backend && npm install
cd ../frontend && npm install 
```

Second, run backend server: 
```
cd backend && npm start 
```
Finally, run react fronted: 
```
cd frontend && npm start
```
